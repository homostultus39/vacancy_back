services:

  redis:
    image: "redis:alpine"
    container_name: 'redis'
    ports:
      - "6379:6379"
    command: ["redis-server", "--requirepass", "jwttemp"]
    environment:
      - REDIS_PASSWORD=jwttemp